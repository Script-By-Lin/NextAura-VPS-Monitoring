[Definition]
# Matches FastAPI structured JSON logs with failed login events
failregex = "level"\s*:\s*"WARN".*"message"\s*:\s*"(?:Failed login attempt|Authentication failed)".*"client_ip"\s*:\s*"<HOST>"
ignoreregex =
